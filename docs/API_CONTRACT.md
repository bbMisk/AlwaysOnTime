# API Contract (draft)

- `GET /health` -> `{ "status": "ok" }`
- `GET /api/status` -> overall service status (derived from MTA; placeholder returns stub)
- `GET /api/accessibility` -> station elevator/escalator availability (todo)
- `GET /api/alerts` -> active alerts (todo)
